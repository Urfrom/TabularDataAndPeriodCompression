# TabularDataAndPeriodCompression
As name suggests, this project deals with timed data.
The data is spread amoung 12 periods, the goal is to compress the data into half-a-years and quartiles.
To achive the result, every 4 (or 6) data samples are merged into one, with many more features.
